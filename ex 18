import math

ang = float(input('Digite o seu angulo: '))

angn = math.radians(ang)

coss = math.acos(angn)

sen = math.asin(angn)

tan = math.atan(angn)

print('Seu seno é: {:.2f}\nSeu cosseno é: {:2f}\nSua tangente é: {:.2f}'.format(sen, coss, tan))
